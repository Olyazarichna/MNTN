
import styles from "./BlogHero.module.scss";

const BlogHero = () => {
  return (
    <section className={styles.blogContainer}>
           {/* <BlogNavigation  activeTab={activeTab} handleTabClick={handleTabClick} /> */}
    </section>
  );
};

export default BlogHero;
